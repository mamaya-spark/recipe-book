module.exports = (sequelize) => {
  const ShoppingList       = sequelize.import('../models/ShoppingList');
  const ShoppingIngredient = sequelize.import('../models/ShoppingIngredient');

  return {

  }
}
